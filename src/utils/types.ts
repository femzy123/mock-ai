export interface Interview {
  mockId: string;
  jsonMockResp: string;
  jobPosition: string;
  jobDesc: string;
  jobExperience: string;
  createdBy: string;
}

export interface MockQuestions {
  question: string;
  answer: string;
}